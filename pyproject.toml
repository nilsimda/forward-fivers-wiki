[project]
name = "forward-fivers-wiki-data"
version = "0.1.0"
description = "Binary data extraction scripts for MHFG G1 wiki"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false
